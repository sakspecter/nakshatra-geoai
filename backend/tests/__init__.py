"""Tests for the backend. Run from repo root with:

    python -m pytest backend/tests -q

PYTHONPATH should include ``backend`` so ``app`` is importable, or install the
package in editable mode.
"""
